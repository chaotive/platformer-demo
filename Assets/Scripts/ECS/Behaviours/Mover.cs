using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class Mover : MonoBehaviour {

    public float amount = 0.1f;
    public float max = 3;

    private bool movingRight = true;
    private bool movingUp = true;

	void Update () {
        var x = transform.position.x;
        var y = transform.position.y;

        if (x > max) movingRight = false;
        else if (x < -max) movingRight = true;

        if (y > max) movingUp = false;
        else if (y < -max) movingUp = true;

        if (movingRight) x += amount * Time.deltaTime;
        else x -= amount * Time.deltaTime;

        if (movingUp) y += amount * Time.deltaTime;
        else y -= amount * Time.deltaTime;

        transform.position = new Vector2(x, y);
	}
}
*/
 